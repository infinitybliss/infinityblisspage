import type { Review } from "@/types/review";

/**
 * Keep this list empty until Infinity Bliss has real client reviews.
 * The homepage Reviews section stays hidden while the array is empty.
 */
export const reviews: Review[] = [];
